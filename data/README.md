# Data

Runtime capture data lives here.

- `captures/latest/` - most recent scan images written by `scan.py`.
- `calibration/` - calibration image pairs and verification output.
- `focus/` - manual focus/alignment captures.

These files are generated on the Pi and can be deleted when you no longer need a scan or calibration session.
